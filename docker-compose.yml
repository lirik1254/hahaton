version: '3.8'

services:
  app:
    image: ${APP_IMAGE}
    ports:
      - "8080:8080"
    networks:
      - app

# Общая сеть с nginx и фронтом (создаётся командой: docker network create app)
networks:
  app:
    external: true
