spring:
  application:
    name: testProj
  datasource:
    url: jdbc:postgresql://localhost:5432/hahaton
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  servlet:
    context-path: /api